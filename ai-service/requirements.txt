# Base service (always required)
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9
numpy>=1.26
pydicom>=2.4

# Optional: decode JPEG/JPEG2000-compressed DICOM
# pylibjpeg>=2.0
# pylibjpeg-libjpeg>=2.0
# pylibjpeg-openjpeg>=2.0

# Optional real ML engines — install to activate the corresponding slot:
# lungmask>=0.2.20          # lung segmentation on CT (pulls torch, ~2 GB)
# TotalSegmentator>=2.4     # 117-structure CT segmentation (pulls torch)
